#include <stdio.h>

int main() {
    int i,j,rows;
   // printf("Enter number of rows");
   // scanf("%d",&rows);
    for(i=1;i<=rows;i++)
    {
        for(j=rows;j>i;j--)
        {
            printf("  ");
        }
        for(j=1;j<=(2*i)-1;j++)
        {
            if(i!=rows)
            {
                if(j==1||j==(2*i)-1)
                printf("* ");
                else
                printf("  ");
            }
            else
            {
            printf("* ");
            }
        }
        printf("\n");
    }

    return 0;
}
